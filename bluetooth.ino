// void bluetoothConnect(){
//   if (SerialBT.available()) {
//     char received = SerialBT.read();      // Read incoming Bluetooth data
//     Serial.write(received);               // Write to Serial Monitor
//     SerialBT.write(received);             // Echo back to sender
//   }
// }